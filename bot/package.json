{
  "name": "nexus-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Discord ticket, shop, giveaway and vouch bot",
  "scripts": {
    "start": "node --experimental-sqlite dist/index.js",
    "dev": "node --watch --experimental-sqlite --experimental-strip-types src/index.ts",
    "test": "node --test dist/util.test.js",
    "build": "tsc --pretty false"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
